server:
  port: 8888
  servlet:
    context-path: /config

spring:
  application:
    name: eo.config
  profiles:
    active: native
  cloud:
    config:
      server:
        encrypt:
          enabled: false
  security:
    user:
      name: eo-config-admin
      password: eo-config-231005

excitingobject:
  security:
    enable: false
